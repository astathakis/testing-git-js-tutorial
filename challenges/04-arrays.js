/*

## Arrays #4
1. create fruits array and store fruit values
2. setup last value as a number random
3. assign first fruit to the variable
4. reassign last array item to the the actual fruit
5. log entire array as well as first fruit

*/

const fruits = ['apple', 'orange', 'melon'];

let random = 12;
fruits[2] = random;

fruits[0] = random;

fruits[2] = 'apple';

console.log(fruits[0], fruits);
